| **Module**                 | **Purpose / Definition**                                                | **Example Code Snippet**                                                                                                                                            |
| -------------------------- | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 🗂️ **os**                 | Interacts with the operating system — manage files, folders, and paths. | `python\nimport os\nprint(os.getcwd())  # current directory\nos.mkdir("new_folder")`                                                                                |
| 🗣️ **pyttsx3**            | Converts text to speech (works offline).                                | `python\nimport pyttsx3\nengine = pyttsx3.init()\nengine.say("Hello!")\nengine.runAndWait()`                                                                        |
| 🎙️ **speech_recognition** | Converts spoken words to text using microphone or audio input.          | `python\nimport speech_recognition as sr\nr = sr.Recognizer()\nwith sr.Microphone() as source:\n    audio = r.listen(source)\n    print(r.recognize_google(audio))` |
| 🌐 **webbrowser**          | Opens websites in your default web browser.                             | `python\nimport webbrowser\nwebbrowser.open("https://www.google.com")`                                                                                              |
| 🕒 **datetime**            | Handles date and time — get, format, or manipulate them.                | `python\nimport datetime\nnow = datetime.datetime.now()\nprint(now.strftime("%H:%M:%S"))`                                                                           |
| 📚 **wikipedia**           | Fetches summaries and information from Wikipedia.                       | `python\nimport wikipedia\nprint(wikipedia.summary("Python language", sentences=2))`                                                                                |
| 🎥 **cv2 (OpenCV)**        | Used for image and video processing tasks.                              | `python\nimport cv2\nimg = cv2.imread("image.jpg")\ncv2.imshow("Image", img)\ncv2.waitKey(0)\ncv2.destroyAllWindows()`                                              |
